# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Build artifacts (生成プラグインディレクトリ用)
*.so
*.dylib
*.dll
*.h
build/
dist/
# Native CLI binaries produced by Makefile targets (build-logger / build-doctor)
# Anchor to repo root with leading slash so the cmd/claude-code-doctor/ source
# directory is not also matched.
/claude-code-security-logger-*
/claude-code-doctor
/claude-code-doctor-*
# Release artifacts (generated by `make package`)
checksums.sha256
*.sig
*.cert
sbom.cdx.json

# Go
vendor/
*.test
*.out
coverage.txt

# Claude Code workspace
.claude/skills/plugin-debug-workspace/

# Serena MCP local state
.serena/
